// Placeholder for future interactivity
document.getElementById('entry-form')?.addEventListener('submit', function (e) {
  e.preventDefault();
  alert("Journal entry submitted! (Not yet connected to a backend)");
});